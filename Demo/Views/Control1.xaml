<UserControl x:Class="Demo.Views.Control1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:constants="clr-namespace:Demo.Constants"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <DockPanel>
        <Button Content="Navigate" Command="{Binding NavigateCommand}" CommandParameter="{x:Static constants:ViewNames.Control2}" DockPanel.Dock="Bottom" />

        <Viewbox DockPanel.Dock="Top">
            <Label Content="Control1" />
        </Viewbox>
    </DockPanel>
</UserControl>
